apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-api
  namespace: letiendung
  labels:
    app: order-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-api
  template:
    metadata:
      labels:
        app: order-api
    spec:
      containers:
      - name: order-api
        image: tiendung209/jobportal-backend:v1
        ports:
        - containerPort: 80
          name: http